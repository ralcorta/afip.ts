{
  "name": "@arcasdk/integration-consumer-smoke",
  "version": "0.0.0",
  "private": true,
  "description": "Smoke / integration tests against the published @arcasdk/core tarball (npm pack), not workspace source.",
  "scripts": {
    "test": "jest --config jest.config.cjs",
    "test:integration": "ENABLE_INTEGRATION_TESTS=true jest --config jest.config.cjs --runInBand"
  },
  "dependencies": {
    "@arcasdk/core": "file:vendor/core.tgz"
  },
  "devDependencies": {
    "@jest/globals": "^30.0.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.8.0",
    "dotenv": "^17.0.0",
    "jest": "^30.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^6.0.0"
  },
  "overrides": {
    "axios": "^1.18.1",
    "form-data": "^4.0.6"
  }
}
